* text eol=lf
*.png binary
*.gif binary
*.mp4 binary
*.zip binary
# Keep the mesher's emitted log spacing as evidence.
gpu-benchmark/mesh-*/log.* -whitespace
sequence/**/diagnostic-raw/** -whitespace
