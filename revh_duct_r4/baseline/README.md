Historical D3/T3/R2/B1 source and regression fixtures.

These fixtures reproduce the blocked socket defect. Do not print the old D3
projects here. Use the D4 projects one directory above. The baseline native
assembly is the exact model against which the D4-only geometry change was
checked; it includes fit revisions newer than the earlier public C1 snapshot.
